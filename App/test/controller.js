/*will be testing the controllers using both stubs and the database
approach*/

const expect = require('chai').expect

const request = require('request')

const sinon = require('sinon')

/*using stubs we just remove some of the functionalities 
to test how the page will be working*/